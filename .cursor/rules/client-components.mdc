---
description: How to write react client components
globs: 
alwaysApply: false
---
# General
* Always implement interface props
* Never use `React.*`, import from react individually
* Always use normal functions, not arrow functions

# Location
* Components that are being reused should be stored in the `src/components` directory, and non-reused should be stored co-locally

# Props
* Dhould be named the same as the component but with a "Props" suffix
* Should always extended the underlying components props
* Example:
```js
"use client";

import { cn } from "@fundlevel/ui/lib/utils"
import type { ComponentPropsWithoutRef } from "react"

export interface CustomDivProps
  extends ComponentPropsWithoutRef<"div"> {
  companyId: number;
}

export function CutomDiv({ companyId, className ...props }: CompaniesidebarProps) {

  return (
    <div className={cn("bg-redd-500", className)} {...props} >
      <div>cutsom content</div>
    </div>
  );
}
```

# Fetching
* Utlize react query and the API sdk [how-to-use-sdk-client.mdc](mdc:.cursor/rules/packages/sdk/how-to-use-sdk-client.mdc)
